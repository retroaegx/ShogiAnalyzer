"""
Minimal sqlite3-based persistence.

The design document recommends SQLAlchemy/SQLModel + Alembic, but the minimal build
uses stdlib sqlite3 to keep the prototype runnable with fewer moving parts.
"""

